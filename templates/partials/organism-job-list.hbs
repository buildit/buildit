<section class="grav-c-job-list">
  <header>
    <h2>{{location.city}}, {{location.country}}</h2>
  </header>
  <ul>
    {{#each jobs}}
    <li>
      {{> molecule-job-card }}
    </li>
    {{/each}}
  </ul>
</section>
