{{#extend "page"}}
{{#content "main"}}
 
  <h1>{{title}}</h1>
 
  <section>
    <div class="grav-c-two-columns-block">
      {{#each locations}}
        {{>molecule-location-card}} 
      {{/each}}
    </div>
  </section>

{{/content}}
{{/extend}}
